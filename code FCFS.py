#!usr/bin/env python

total_t=0
bur_time=[]
arr_time=[]
def myFCFSfunc(processes.index)
   newprocesses=list[]
   element=processes[index]
   length=len(processes)
   count=0;   
for j in range (0,length):
	a_time=input("Arrival Time:")
	arrival_time.append(a_time)
	b_time=input("Burst Time:")
	burst_time.append(b_time)
	processes[burst_time[j]] = [j+1 , arrival_time[j]]

print ("Arrival Time           Burst Time")
for j in range (0,length):
	print (arrival_time[0], "\t\t\t" , burst_time[j])
	arr_time.sorted_t()
total_t = processes.get(bur_time[0])[1]
	total = processes.get(1)[0]
    min = total
 if(total>0):
	print '0 -------' , total 
for i in range (1,length+1):
	total = total + processes.get(i)[1]
	print min , "________" , total
	min = total
 for j in range(length)
    arr_time=processes[j]
if arr_time < elem or (arr_time==elem && j<=index)
	newprocesses.append(o)
	count+=length

